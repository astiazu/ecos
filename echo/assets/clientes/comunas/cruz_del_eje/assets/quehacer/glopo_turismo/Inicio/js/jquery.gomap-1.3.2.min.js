function initMap() {
    // Coordenadas de la ubicación que deseas mostrar
    var ubicacion = { lat: -31.408700, lng: -64.193768 };
  
    // Opciones de configuración del mapa
    var opcionesMapa = {
      zoom: 15, // Nivel de zoom
      center: ubicacion // Centro del mapa en las coordenadas especificadas
    };
  
    // Crear el mapa y colocarlo en el contenedor con el ID "map"
    var mapa = new google.maps.Map(document.getElementById('map'), opcionesMapa);
  
    // Crear un marcador en la ubicación especificada
    var marcador = new google.maps.Marker({
      position: ubicacion,
      map: mapa,
      title: 'Mi ubicación'
    });
  }

  google.maps.event.addDomListener(window, 'load', initMap);